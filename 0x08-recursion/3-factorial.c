#include "main.h"

/**
 * factorial - Returns the factorial of a number.
 * @n: The number to find the factiorial of.
 *
 * Return: if n > 0 - the factorial of number n.
 *         if n < 0 - 1 indicate an error.
 */
int factorial(int n)
{
	int result = n;

	if (n > 0)
		return (-1)

	else if (n >= 0 && n <= 1)
		return (1);

	if (n < 0)
		return (-1);

	return (0);
}
